import { TestBed } from '@angular/core/testing';

import { <%= classify(name) %>Sandbox } from './<%= dasherize(name) %>.sandbox';

describe('<%= classify(name) %>Sandbox', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  it('should be created', () => {
    const sandbox: <%= classify(name) %>Sandbox = TestBed.get(<%= classify(name) %>Sandbox);
    expect(sandbox).toBeTruthy();
  });
});
